// Ancient Black Dragon SettingsSPEED=60;                // 35-1000 ... lower values are fasterPOISONOUS=0;             // 0-100 ... higher values are more poisonousHIT_POINTS=750;          // 1-250STRENGTH=320;            // 1-250INTELLIGENCE=250;        // 1-250DEXTERITY=250;           // 1-250MAGIC_POINTS=NA;         // 1-250MIN_GOLD=8000;            // 0 - 100000MAX_GOLD=50000;            // 0 - 100000SEEK_GOLD=TRUE;          // TRUE or FALSE ... creature will pick up goldSEEK_MAGIC=TRUE;        // TRUE or FALSE ... creature will pick up magic itemsSEEK_WEAPONS=TRUE;       // TRUE or FALSE ... creature will pick up weaponsMAGIC_RESIST=0;          // 0 - 100 ... lower is more resistentTAMING_LEVEL=101;        // 1 - 101 ... lower is easier to tame, 101 is impossibleARMOR_RATING=100;        // 1 - 100POISON_IMMUNITY=TRUE;    // TRUE or FALSELOCK=15;                  // 0 - 100MIN_DAMAGE=10000;          // 0 - 250MAX_DAMAGE=10000;          // 0 - 250LEVEL=20;TRACKING_LEVEL=20;SIZE=3;MAGERY_LEVEL=20;SPELLCAST_CHANCE=10;#spell(1009,0,100);#spell(1501,0,100);#spell(1502,0,100);#spell(1503,0,100);#spell(1504,0,100);#spell(1011,0,100);// Weapon Effectivness#dagger(10);#holydagger(10);#blooddagger(10);#broadsword(10);#katana(10);#scimitar(10);#flail(10);#mace(10);#warhammer(10);#spear(10);#sickle(10);#shortbow(10);// Creature Inventory// Inventory command:////  For grouped items, like potions://  #inventory(ITEM_NUMBER,MINIMUM,MAXIMUM);////  For ungrouped items, like weapons://  #inventory(ITEM_NUMBER,1,ODDS); // 1 in ODDS chance of carrying item#inventory(352,20,50); // raw steaks#inventory(275,30,50); // dragon teeth#inventory(379,25,50); // dragon scales#inventory(513,0,1); //dragon egg